# Public API contract snapshot

Copied from `rizzomichaelg/snappycoin-backend` commit `b2a7425`, `docs/pud-openapi.yaml`, for this coordinated release. Update this snapshot from the backend whenever the browser contract changes. Backend CI independently verifies the frontend against its canonical contract.
